{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '=== Session Start: Reading CLAUDE.md, AI_HANDOFF.md, CHANGELOG.md, DEVELOPMENT_FLOW.md, PROJECT_ARCHITECTURE.md, and HANDOFF_PROMPT.md ===' && echo '--- CLAUDE.md ---' && cat CLAUDE.md && echo '--- AI_HANDOFF.md ---' && cat AI_HANDOFF.md && echo '--- CHANGELOG.md ---' && cat CHANGELOG.md && echo '--- DEVELOPMENT_FLOW.md ---' && cat DEVELOPMENT_FLOW.md && echo '--- PROJECT_ARCHITECTURE.md ---' && cat PROJECT_ARCHITECTURE.md && echo '--- HANDOFF_PROMPT.md ---' && cat .claude/HANDOFF_PROMPT.md"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "edit|write|bash",
        "hooks": [
          {
            "type": "command",
            "command": "BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null); if [ \"$BRANCH\" = \"main\" ] || [ \"$BRANCH\" = \"develop\" ]; then echo \"Error: Refusing to make changes to branch '$BRANCH'. Please switch to a feature branch.\"; exit 1; fi"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '=== Session End: Reading CLAUDE.md, AI_HANDOFF.md, CHANGELOG.md, DEVELOPMENT_FLOW.md, PROJECT_ARCHITECTURE.md, and HANDOFF_PROMPT.md ===' && echo '--- CLAUDE.md ---' && cat CLAUDE.md && echo '--- AI_HANDOFF.md ---' && cat AI_HANDOFF.md && echo '--- CHANGELOG.md ---' && cat CHANGELOG.md && echo '--- DEVELOPMENT_FLOW.md ---' && cat DEVELOPMENT_FLOW.md && echo '--- PROJECT_ARCHITECTURE.md ---' && cat PROJECT_ARCHITECTURE.md && echo '--- HANDOFF_PROMPT.md ---' && cat .claude/HANDOFF_PROMPT.md && echo '=== Session End: Please update AI_HANDOFF.md with your handoff for the next AI ===' && echo 'Current AI_HANDOFF.md:' && cat AI_HANDOFF.md"
          }
        ]
      }
    ]
  }
}
